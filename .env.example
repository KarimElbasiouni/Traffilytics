# Copy to .env and fill in values. Never commit .env.

# Hugging Face token for downloading DRIFT (optional if dataset is public/cached)
HF_TOKEN=

# Data and DB (paths relative to repo root unless absolute)
DATA_ROOT=./data
DB_URL=sqlite:///./data/traffilytics.db
